services:
  llmproxy:
    image: scyslz/llmproxy
    container_name: llmproxy
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: production
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs